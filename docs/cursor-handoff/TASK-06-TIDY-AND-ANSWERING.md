# Cursor 独立任务：完成知识库存储、检索和多方案回答

仓库 `D:\Dev\VideoCaptioner`。当前已有 Tidy 兼容 Markdown、SQLite FTS、中文 n-gram+IDF 回退和
`answer-tidy`。本任务要在全量课程/PDF 完成后验证并完善消费层，不能只做假数据测试。

先确定本机/仓库是否存在用户所说具体 Tidy 产品的明确链接、API、容器或配置。能确定则实现 provider
adapter、配置模板、幂等导入、更新/删除和真实冒烟；不能确定则不得冒充外部接入已完成，必须保留本地
实现，并交付通用 JSONL/Markdown 导出、清晰 provider 接口和以后接入说明。

索引全量 P06，验证课程、案例、speaker、时间/页码、evidence、安全标签和 Prompt 版本可查询。构建不少于
20 个真实问题的测试集，覆盖事实、相似案例、多个解释、拒绝边界、隐私、醉酒、年龄、不同关系阶段。
每个回答必须包含客观事实、至少 3 种解释、至少 3 个行动方案，每方案至少 3 种说法，引用有效知识 ID，
并明确不确定性和安全停止条件。禁止把讲师观点当事实或输出施压/欺骗推进建议。

交付：数据库/导出、可重复命令、20 问结果与 QA、性能和命中率报告、README、测试、commit 和 push。

