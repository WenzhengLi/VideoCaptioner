# Cursor 独立任务：完成 C006–C010 和前 10 课回归

仓库 `D:\Dev\VideoCaptioner`，批次 `BATCH-20260715-001`，Python 为 `.venv\Scripts\python.exe`。
C001–C005 已完成 v002；本任务完成 C006–C010。每次 Cursor 子任务使用全新上下文和
`-p --force --sandbox disabled --approve-mcps --trust --model auto`，禁止 `--resume`。

先审计进程，避免重复启动。完成五课视频分析和原始 QA，归档运行号统一为
`RUN-20260715-001-V001`。随后以 `WaveId=C006-C010` 依次完成 P01–P06。每课 P01/P02/P03 QA 必须 pass；
每案例 P04/P05/P06 QA 必须 pass；单个失败只补跑该课/案例。保留 speaker cluster，P03 全量覆盖，证据 ID
在案例内；拒绝、不适、醉酒、年龄和隐私风险不能变成推进技巧。

完成后重建 `data/tidy/knowledge.db`，运行中文检索、多方案回答和全量 pytest。生成
`docs/evaluation/knowledge-C001-C010.md`，逐课列出 segments、speaker、unknown 比例、案例、未分配比例、
安全标签、条目、Markdown、QA、耗时和重试。抽检每课至少 2 案例。发现系统性问题时复制 Prompt 为 v003，
不要改写 v002；代码/脚本要支持可配置版本，并回归 C001–C010。完整度优先，不能以减少重复替代完整性。

交付：C006–C010 全部产物与波次完成标记、10 课质量报告、必要的 v003/代码/测试、干净提交和 push。

