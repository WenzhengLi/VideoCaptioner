# Cursor 独立任务：冻结 Prompt 后处理全部唯一视频和 PDF

工作区 `D:\Dev\VideoCaptioner`；来源根目录位于 E 盘课程目录。开始前读取 catalog、前 20 课最终报告、
冻结 Prompt 和 STATUS。已知 C078=C068，未编号 `烽爆迷情2024.MP4`=C012；必须重新以 catalog/hash
证据确认，重复项不得做昂贵处理，但映射必须保留。PDF 为 PDF001。

按小波次串行处理剩余唯一视频。每课独立 Python 进程、独立 Cursor 上下文、有限重试和断点恢复；每课
原始归档后立即 QA，每案例 P04–P06 后立即 QA。维护机器可读成功/失败/补跑清单，任何死机只补跑缺失课。
所有课程使用冻结 Prompt，不在全量过程中无版本地修改规则；若发现致命系统问题，提升新版本并先回归
C001–C020，再继续全量。

为 PDF001 使用独立 PDF 流程：保存页码、文本块、图片/OCR 和来源定位，生成与视频知识条目兼容的
source/evidence。不得把 PDF 当视频，不得生成无法追溯页码的纯摘要。增加 PDF 单元和真实文件冒烟测试。

最终生成全量 manifest：来源总数、唯一视频、重复、PDF、成功、失败、每课运行号、Prompt 版本和 QA。
失败必须为零才能声明完成。data/jobs 不提交；代码、Prompt、测试、文档提交并 push。

