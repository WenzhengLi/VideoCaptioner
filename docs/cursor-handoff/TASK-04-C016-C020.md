# Cursor 独立任务：完成 C016–C020、20 课回归并冻结 Prompt

仓库 `D:\Dev\VideoCaptioner`。读取 STATUS 和前 15 课报告，使用最新 Prompt，新建 WaveId
`C016-C020`。逐课完成视频分析、原始 QA、P01–P06 和逐案例 QA；失败断点补跑，不并发写同一输出。

20 课审查除常规完整度和证据外，必须专项验证：无固定时长的片头/片尾；课板左右移动和 ROI 留边；稳定
区间只做代表帧 OCR；相同帧 OCR 缓存；无文字→无文字但画面大变时的中点检查；短暂文字不漏；内存和
磁盘缓存生命周期；speaker 聚类；长视频案例边界；拒绝/隐私/醉酒/年龄风险召回。

发现问题则提升 Prompt/代码版本，回归 C001–C020。通过后把版本标记 frozen，在 Prompt README 和
CHANGELOG 写冻结依据、指标、已知限制。生成 `docs/evaluation/knowledge-C001-C020.md`，重建索引并执行
不少于 20 个问题的回答验收。全量测试通过后提交 push。

完成标准：前 20 课全量回归通过；冻结版本有明确版本号；旧结果未覆盖；报告可证明完整度没有退化。

